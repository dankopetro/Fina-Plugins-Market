# 📟 Plugin Telecentro SEI800TC1

Este plugin permite el control total del decodificador SEI800TC1 de Telecentro (y modelos similares) mediante comandos de voz.

## ⚠️ Nota de Seguridad Importante

Por razones de seguridad y para evitar bloqueos en GitHub, los archivos de certificado y clave privada (**`cert.pem`** y **`key.pem`**) **no han sido incluídos** en este repositorio público.

### Requisitos para el funcionamiento:
Para que este plugin funcione, debes copiar tus propios archivos `cert.pem` y `key.pem` dentro de esta carpeta. Estos archivos son necesarios para la comunicación SSL segura con el dispositivo.

Si ya tienes una instalación local funcionando, simplemente copia estos dos archivos desde tu carpeta de configuración original a la carpeta de este plugin.
